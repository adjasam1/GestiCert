/*import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { InfoCertificateComponent } from './info-certificate.component';

describe('InfoCertificateComponent', () => {
  let component: InfoCertificateComponent;
  let fixture: ComponentFixture<InfoCertificateComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ InfoCertificateComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(InfoCertificateComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});*/
